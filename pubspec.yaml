name: jobinja_app
description: "A Flutter Jobinja client with real API integration."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
